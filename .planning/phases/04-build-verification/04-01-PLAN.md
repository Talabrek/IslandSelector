---
phase: 04-build-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Maven build completes without errors"
    - "All Java source files are present in src/"
    - "Resource files (addon.yml, config.yml, en-US.yml) exist"
  artifacts:
    - path: "src/main/java"
      provides: "Java source directory"
    - path: "src/main/resources/addon.yml"
      provides: "BentoBox addon configuration"
    - path: "src/main/resources/config.yml"
      provides: "Plugin configuration"
    - path: "src/main/resources/locales/en-US.yml"
      provides: "English locale"
    - path: "pom.xml"
      provides: "Maven build configuration"
  key_links:
    - from: "pom.xml"
      to: "src/main/java"
      via: "Maven source directory"
      pattern: "src/main/java"
---

<objective>
Verify the restructured Maven project builds correctly and all files are in place.

Purpose: Confirm Phase 3 restructure succeeded and project is ready for development
Output: Verification report confirming build success and file integrity
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Java source file count</name>
  <files>src/main/java</files>
  <action>
    Count all Java source files in src/main/java recursively.

    Run: find src/main/java -name "*.java" | wc -l

    The roadmap states 85 files should be present. Document the actual count.
    If count differs from 85, list all files to identify what's present.

    Note: The count may have been estimated. The key verification is that
    all expected addon classes exist, not an exact number.
  </action>
  <verify>Command completes and outputs a number</verify>
  <done>Java file count documented, reasonable for a BentoBox addon</done>
</task>

<task type="auto">
  <name>Task 2: Verify resource files present</name>
  <files>src/main/resources</files>
  <action>
    Verify the three required resource files exist:

    1. src/main/resources/addon.yml - BentoBox addon descriptor
    2. src/main/resources/config.yml - Plugin configuration
    3. src/main/resources/locales/en-US.yml - English translations

    Run: ls -la src/main/resources/ && ls -la src/main/resources/locales/

    Confirm each file exists and is non-empty.
  </action>
  <verify>ls commands show all three files with non-zero size</verify>
  <done>addon.yml, config.yml, and en-US.yml all present and non-empty</done>
</task>

<task type="auto">
  <name>Task 3: Run Maven build verification</name>
  <files>pom.xml, target/</files>
  <action>
    Run Maven clean compile to verify the project builds:

    cd C:/Users/Administrator/Desktop/VSCode/IslandSelector && mvn clean compile

    Expected outcome: BUILD SUCCESS

    If build fails:
    - Check error messages for missing dependencies
    - Verify pom.xml is valid XML
    - Check Java version compatibility

    Do NOT attempt to fix issues - this is verification only.
    Document any errors for potential Phase 4.1 if needed.
  </action>
  <verify>mvn clean compile exits with code 0 and shows BUILD SUCCESS</verify>
  <done>Maven build succeeds without errors, confirming project structure is valid</done>
</task>

</tasks>

<verification>
All three requirements verified:
- VER-01: mvn clean compile succeeds (Task 3)
- VER-02: Java source files present in src/ (Task 1)
- VER-03: Resource files present (Task 2)
</verification>

<success_criteria>
1. Java file count documented (expected ~85, actual count recorded)
2. All three resource files confirmed present and non-empty
3. mvn clean compile completes with BUILD SUCCESS
4. No errors in build output requiring follow-up
</success_criteria>

<output>
After completion, create `.planning/phases/04-build-verification/04-01-SUMMARY.md`

Include:
- Actual Java file count
- Resource file verification results
- Maven build output (success/failure)
- Any observations about project state
</output>
