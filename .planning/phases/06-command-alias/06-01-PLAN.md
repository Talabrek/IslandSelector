---
phase: 06-command-alias
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/world/bentobox/islandselector/commands/IslandSelectorCommand.java
autonomous: true

must_haves:
  truths:
    - "Running /map opens the island selector GUI"
    - "Running /map with any subcommand works identically to /islandselector"
    - "/map slots opens slot selection GUI"
    - "/map admin reload reloads configuration"
  artifacts:
    - path: "src/main/java/world/bentobox/islandselector/commands/IslandSelectorCommand.java"
      provides: "Command with map alias"
      contains: '"map"'
  key_links:
    - from: "IslandSelectorCommand constructor"
      to: "BentoBox CompositeCommand"
      via: "super() call with map alias"
      pattern: 'super\(addon,.*"map"'
---

<objective>
Add `/map` as a command alias for `/islandselector`

Purpose: Provide players a shorter, more intuitive command to open the island selector GUI
Output: Modified IslandSelectorCommand.java with "map" alias in constructor
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-command-alias/06-RESEARCH.md

# Source file to modify
@src/main/java/world/bentobox/islandselector/commands/IslandSelectorCommand.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add map alias to IslandSelectorCommand constructor</name>
  <files>src/main/java/world/bentobox/islandselector/commands/IslandSelectorCommand.java</files>
  <action>
Modify the constructor call on line 20 to add "map" as a fourth alias parameter.

Change from:
```java
super(addon, "islandselector", "is", "isgrid");
```

Change to:
```java
super(addon, "islandselector", "is", "isgrid", "map");
```

This is a single-line change. BentoBox's CompositeCommand handles all alias registration automatically through the constructor varargs. No additional configuration, registration, or plugin.yml changes are needed.

All subcommands (/map slots, /map admin, /map help, etc.) will work automatically because they inherit from the parent command.
  </action>
  <verify>
1. `mvn clean package -q` succeeds without errors
2. Search codebase confirms "map" appears in IslandSelectorCommand constructor
  </verify>
  <done>
IslandSelectorCommand constructor includes "map" as fourth alias parameter and project builds successfully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build and verify JAR</name>
  <files>target/IslandSelector-1.0.0.jar</files>
  <action>
Build the addon with Maven to verify the change compiles correctly:

```bash
mvn clean package -q
```

Verify the JAR was created at target/IslandSelector-1.0.0.jar

The built JAR will have the /map alias registered when loaded by BentoBox. No runtime testing is possible without a live server, but the build verification confirms correct Java syntax.
  </action>
  <verify>
1. Build completes without errors
2. JAR file exists at target/IslandSelector-1.0.0.jar
  </verify>
  <done>
Project builds successfully with the map alias change, producing a deployable JAR.
  </done>
</task>

</tasks>

<verification>
1. `grep -n '"map"' src/main/java/world/bentobox/islandselector/commands/IslandSelectorCommand.java` shows the alias in the constructor
2. `mvn clean package -q` completes successfully
3. `ls target/IslandSelector-*.jar` shows JAR was built
</verification>

<success_criteria>
1. IslandSelectorCommand.java constructor line reads: `super(addon, "islandselector", "is", "isgrid", "map");`
2. Project builds without compilation errors
3. JAR artifact produced in target/ directory
</success_criteria>

<output>
After completion, create `.planning/phases/06-command-alias/06-01-SUMMARY.md`
</output>
