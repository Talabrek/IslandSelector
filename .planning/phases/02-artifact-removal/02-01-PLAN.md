---
phase: 02-artifact-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent.py (delete)
  - client.py (delete)
  - progress.py (delete)
  - prompts.py (delete)
  - security.py (delete)
  - test_security.py (delete)
  - autonomous_agent_demo.py (delete)
  - requirements.txt (delete)
  - __pycache__/ (delete folder)
  - prompts/ (delete folder)
  - generations/island_selector/*.py (delete 9 files)
autonomous: true

must_haves:
  truths:
    - "No Python files (*.py) exist anywhere in project"
    - "No __pycache__ folder exists"
    - "No prompts/ folder exists"
    - "requirements.txt does not exist"
  artifacts:
    - path: "agent.py"
      state: "deleted"
    - path: "__pycache__/"
      state: "deleted"
    - path: "prompts/"
      state: "deleted"
    - path: "generations/island_selector/*.py"
      state: "deleted (9 files)"
  key_links: []
---

<objective>
Delete all Python files and related artifacts from root and generations/island_selector directories.

Purpose: Remove AI agent tooling and Python development scripts that are not part of the Java addon.
Output: Clean project with no Python files or cache folders.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-artifact-removal/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete root Python files and folders</name>
  <files>
    agent.py, client.py, progress.py, prompts.py, security.py,
    test_security.py, autonomous_agent_demo.py, requirements.txt,
    __pycache__/, prompts/
  </files>
  <action>
Delete all Python-related files from the project root directory:

1. Delete Python source files:
   - agent.py
   - client.py
   - progress.py
   - prompts.py
   - security.py
   - test_security.py
   - autonomous_agent_demo.py

2. Delete requirements.txt (Python dependencies file)

3. Delete __pycache__/ folder (Python bytecode cache)

4. Delete prompts/ folder (AI prompts - 3 files inside)

All files are untracked - use regular file deletion (rm), not git rm.
Use -f flag to avoid prompts for confirmation.
Use -rf for folders.
  </action>
  <verify>
Run verification commands:
- `ls *.py 2>/dev/null` should return empty (no matches)
- `ls requirements.txt 2>/dev/null` should return empty
- `test -d __pycache__ && echo EXISTS || echo DELETED` should show DELETED
- `test -d prompts && echo EXISTS || echo DELETED` should show DELETED
  </verify>
  <done>
No Python files exist in project root.
No __pycache__ or prompts/ folders exist in project root.
requirements.txt does not exist.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete Python scripts from generations/island_selector</name>
  <files>
    generations/island_selector/add_method.py
    generations/island_selector/check_neighborhood.py
    generations/island_selector/check_session42.py
    generations/island_selector/check_tests.py
    generations/island_selector/find_later_tests.py
    generations/island_selector/find_permission_tests.py
    generations/island_selector/find_simple_tests.py
    generations/island_selector/find_tests.py
    generations/island_selector/get_neighborhood_tests.py
  </files>
  <action>
Delete all 9 Python scripts from generations/island_selector/:

1. Delete using glob pattern: generations/island_selector/*.py
   - add_method.py
   - check_neighborhood.py
   - check_session42.py
   - check_tests.py
   - find_later_tests.py
   - find_permission_tests.py
   - find_simple_tests.py
   - find_tests.py
   - get_neighborhood_tests.py

All files are untracked - use regular rm, not git rm.
  </action>
  <verify>
Run: `ls generations/island_selector/*.py 2>/dev/null` - should return empty (no matches)
Run: `find generations/island_selector -maxdepth 1 -name "*.py" 2>/dev/null` - should return nothing
  </verify>
  <done>
No Python files exist in generations/island_selector/ directory.
  </done>
</task>

</tasks>

<verification>
Complete Python cleanup verification:

1. Search entire project for Python files:
   `find . -name "*.py" -not -path "./.git/*" 2>/dev/null`
   Expected: No output (no Python files found)

2. Verify no Python cache folders:
   `find . -type d -name "__pycache__" 2>/dev/null`
   Expected: No output

3. Verify prompts folder removed:
   `test -d prompts && echo FAIL || echo PASS`
   Expected: PASS
</verification>

<success_criteria>
- No *.py files exist anywhere in the project tree
- No __pycache__/ folder exists
- No prompts/ folder exists
- requirements.txt does not exist
- Requirements REM-01 through REM-05 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/02-artifact-removal/02-01-SUMMARY.md`
</output>
